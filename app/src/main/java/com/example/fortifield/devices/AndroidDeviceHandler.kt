package com.example.fortifield.devices

//could be a class that implements DeviceHandler and handles features specific to Android devices.
class AndroidDeviceHandler {
    fun handleDevice() {

    }
}